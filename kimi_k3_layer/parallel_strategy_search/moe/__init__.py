"""TRT-LLM-faithful Kimi-K3 MoE layer baseline (TP reference vs CP emulation).

No heavy imports here; import ``layer`` or run ``bench_layer.py`` under mpirun
inside the ``trt-k3-prod`` container. See README.md.
"""
